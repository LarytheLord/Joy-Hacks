[sandbox]
# Resource constraints
memory_limit = 512m
cpu_shares = 512
process_limit = 50

# Security policies
readonly_filesystem = true
allowed_syscalls = read,write,open,close,stat

# Network restrictions
network_enabled = false

# Capabilities
drop_capabilities = ALL

# Allowed paths
write_allowed_paths = /tmp,/home/coder/work